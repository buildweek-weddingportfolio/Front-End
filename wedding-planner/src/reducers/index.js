import {  } from "../actions";

const initialState = {
    data: [],
    fetchingData: false,
}

export const reducer = (state = initialState, action) => {
    switch(action.type) {
        default: 
            return state;
    }
}